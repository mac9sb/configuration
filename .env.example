# Cloudflare R2 credentials for daily backups
# Copy this file to .env.local and fill in your values:
#   cp .env.example .env.local
#
# To find these values:
#   1. Go to Cloudflare Dashboard > R2 > Overview
#   2. Click "Manage R2 API Tokens" > "Create API Token"
#   3. Copy the Account ID from the R2 overview page

R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_TOKEN_VALUE=
R2_BUCKET=
