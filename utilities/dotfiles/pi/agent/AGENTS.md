# Agent Rules

## Commit Messages

- Never use Conventional Commits (`feat:`, `fix:`, `chore:`, etc.).
- Use Apple-style commit subjects similar to repositories under `github.com/apple`:
  - short, direct, sentence-style summary
  - no type/scope prefix
  - no trailing period
